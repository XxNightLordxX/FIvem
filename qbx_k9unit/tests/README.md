# Moved — see `DEVELOPER_REFERENCE.md` §20

This file's content was condensed and merged into `DEVELOPER_REFERENCE.md`
§20 on 2026-08-25 — how to run the suite, the sandbox pattern, the two
false-alarm cases, what's covered/not covered, and how to add a new spec
were all kept; the glossary and the per-file coverage table's exact,
frequently-stale counts were cut (re-run `./run.sh` yourself for a current
number, as that section itself says).

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
