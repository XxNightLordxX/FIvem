# COMPLEMENTARY_FEATURES.md — merged into FEATURE_IDEAS.md

This file's entire content has been moved into `FEATURE_IDEAS.md` as
"Part B — Complementary features & ecosystem integration" (docs-consolidation
pass, 2026-08-25). Nothing was rewritten beyond adding a couple of
small "docs-pass note" corrections for flags that have since flipped from
`false` to `true` — see that file. Open `FEATURE_IDEAS.md` instead; there is
nothing left here.

**Why this stub exists instead of the file being gone:** the tooling
available for the pass that did this merge could create, write, and edit
files, but had no way to actually delete a file from disk. This stub is
the closest available substitute — please delete this file for real the
next time someone with shell/filesystem access is doing housekeeping here.
