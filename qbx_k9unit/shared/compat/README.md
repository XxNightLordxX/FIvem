# Moved — see `DEVELOPER_REFERENCE.md` §21

This file's content was merged into `DEVELOPER_REFERENCE.md` §21 on
2026-08-25, in full — nothing was cut, since none of it was decision
history. Read that section for how `Config.Compat`'s `override`/`custom`
options work, the adapter-registration contract, the required-methods
table per system/realm, and the resolution order.

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
