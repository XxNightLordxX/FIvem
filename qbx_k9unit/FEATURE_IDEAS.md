# Moved — see `DEVELOPER_REFERENCE.md` §18

This file's content (Part A and Part B) was condensed and merged into
`DEVELOPER_REFERENCE.md` §18 on 2026-08-25 — the ranked list and section
labels (`Part A §N`, `Part A Tier X §N`, `Part B §N`/`item N`) were kept
since they're cited directly from code; the per-item "why/effort/value/
dependency" prose was condensed to one or two lines each, and items already
shipped since this was written are marked as such inline. For features
still actively being built from, see `K9_IDEAS.md` instead — this file was
always pure ideation, not a backlog.

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
