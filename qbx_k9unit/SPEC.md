# Moved — see `DEVELOPER_REFERENCE.md`

This file's content (§1–§14) was merged into `DEVELOPER_REFERENCE.md` on
2026-08-25, along with `PHASE3_SPEC.md`, `PHASE4_SPEC.md`, `PHASE5_SPEC.md`,
`REFACTOR_ROADMAP.md`, `phase2_notes/RESEARCH_ARCHIVE.md`,
`PROJECT_STATUS.md`, `WATCHDOG_LOG.md`, `FEATURE_IDEAS.md`, `DOCS_INDEX.md`,
`locales/README.md`, `tests/README.md`, and `shared/compat/README.md`.

Every section number this file used to contain (§1–§14, and every `item N`/
`#anchor` cited from it) is unchanged in `DEVELOPER_REFERENCE.md` — only the
filename changed. If a code comment or another doc still names this file,
look up the same number in `DEVELOPER_REFERENCE.md` instead; it hasn't
moved.

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file (and the other redirect stubs left by the same pass) from
the repository outright.
