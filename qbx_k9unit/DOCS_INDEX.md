# Removed — see the table at the top of `DEVELOPER_REFERENCE.md`

This resource's documentation was consolidated from twenty files down to
seven on 2026-08-25: `README.md`, `PLAYER_GUIDE.md`, `OPERATOR_RUNBOOK.md`,
`LICENSE.md`, `CHANGELOG.md`, `K9_IDEAS.md`, and `DEVELOPER_REFERENCE.md`
(plus `html/sounds/CREDITS.md`, which `LICENSE.md` calls legally
load-bearing). A per-document index is no longer needed at that scale — see
`DEVELOPER_REFERENCE.md`'s own opening "Where to look for what" table for
the equivalent map into its own sections.

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
