# Moved — see `DEVELOPER_REFERENCE.md` §12

This file's content (§12.0–§12.7) was merged into `DEVELOPER_REFERENCE.md`
on 2026-08-25. Every section number and item number this file used to
contain is unchanged there — only the filename changed. A citation reading
`PHASE3_SPEC.md §12.5.1` (for example) now resolves to
`DEVELOPER_REFERENCE.md §12.5.1`, same number, same content.

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
