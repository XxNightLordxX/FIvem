# Moved — see `OPERATOR_RUNBOOK.md` §7

This file's content (STEP 0 through STEP 8) was merged into
`OPERATOR_RUNBOOK.md` §7 on 2026-08-25, with the same step numbers, so
every code comment citing `rollback/README.md step N` still resolves to
the same step, just in `OPERATOR_RUNBOOK.md` now. Installing/upgrading via
`k9_setup.sh` moved to `OPERATOR_RUNBOOK.md` §6.

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
