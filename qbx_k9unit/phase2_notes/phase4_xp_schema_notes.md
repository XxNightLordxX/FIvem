# XP schema design note — SUPERSEDED

**Consolidated into [`phase2_notes/RESEARCH_ARCHIVE.md`](RESEARCH_ARCHIVE.md#xp-schema)
on 2026-08-25.** `sql/install.sql`'s `k9_progression` table and
`server/progression.lua` are the authoritative, current source for how XP
persistence actually works.

This stub exists because the documentation pass that did this consolidation
had no shell/git tool access to remove the file. **An agent with git access
should run `git rm phase2_notes/phase4_xp_schema_notes.md`.** Every code
comment that used to cite this file by name has already been repointed at
the archive above.
