# Phase 5 features research — SUPERSEDED

**Consolidated into [`phase2_notes/RESEARCH_ARCHIVE.md`](RESEARCH_ARCHIVE.md#phase-5-research)
on 2026-08-25.** `config.lua`'s `Config.Features` block and the
`client`/`server` files for each Phase 5 feature are the authoritative,
current source for what's actually implemented.

This stub exists because the documentation pass that did this consolidation
had no shell/git tool access to remove the file. **An agent with git access
should run `git rm phase2_notes/phase5_features_research.md`.** Every code
comment that used to cite this file by name has already been repointed at
the archive above.
