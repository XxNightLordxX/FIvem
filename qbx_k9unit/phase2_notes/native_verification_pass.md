# Native verification pass — SUPERSEDED

**Deleted from the archive on 2026-08-25 (not merged — fully superseded).**
This file had zero citations anywhere else in the repository except one:
its own note that `server/combat.lua`'s header still used an invalid "no
`apiset` entry means client-only" justification for `SetEntityCanBeDamaged`.
That header has now been corrected in place (2026-08-25) to cite the real,
valid basis instead (a docs.fivem.net search-index badge quote), closing
the one reason this file was being kept. Every other finding in it was
already superseded by `config.lua`'s own later text or restated in the
citing file's own header.

This stub exists because the documentation pass that did this consolidation
had no shell/git tool access to remove the file. **An agent with git access
should run `git rm phase2_notes/native_verification_pass.md`.**
