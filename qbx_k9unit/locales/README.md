# Moved — see `DEVELOPER_REFERENCE.md` §19

This file's content was condensed and merged into `DEVELOPER_REFERENCE.md`
§19 on 2026-08-25 — the shared-key list, the "how to add a key" process,
and the manifest requirements were kept; the "numbers below expire" meta-
commentary and the migration history were cut as superseded (the migration
is complete; see `README.md`'s own locale section for current file/key
counts).

This file is intentionally left as a redirect stub rather than removed,
because the tooling used to perform this consolidation pass had no shell/git
access and could not run `git rm`. Whoever next has shell access should
delete this file outright.
